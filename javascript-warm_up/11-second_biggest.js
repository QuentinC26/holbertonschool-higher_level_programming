#!/usr/bin/node
const process = require('process');
if (Number(process.argv.length) === 2) {
  console.log('0');
} else if (Number(process.argv.length) === 3) {
  console.log('0');
} else {
    PASS
}